# Optional: mount a TLS Secret instead of generating a self-signed pair.
# oc create secret tls tlsbench-server --cert=tls.crt --key=tls.key -n tlsbench
# Then add to the Deployment:
#
# env:
#   - name: TLS_CERT_FILE
#     value: /etc/tls/private/tls.crt
#   - name: TLS_KEY_FILE
#     value: /etc/tls/private/tls.key
#   - name: TLS_CA_FILE
#     value: /etc/tls/private/ca.crt
# volumeMounts:
#   - name: server-tls
#     mountPath: /etc/tls/private
#     readOnly: true
# volumes:
#   - name: server-tls
#     secret:
#       secretName: tlsbench-server
